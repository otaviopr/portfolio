# Content Studio

Open `admin/index.html` in a browser to manage home and project content.

This static portfolio export has no backend, so the editor stores data in browser local storage. Use **Export content** to create a portable JSON backup, and **Import content** to restore it. The current Readymag HTML is not modified automatically; connecting the export to a hosted CMS or publishing flow is the next integration step.
